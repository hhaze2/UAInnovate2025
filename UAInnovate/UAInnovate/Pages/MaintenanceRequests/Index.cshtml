@page
@model UAInnovate.Pages.MaintenanceRequests.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MaintenanceRequests[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaintenanceRequests[0].Issue)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaintenanceRequests[0].IssueLocation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaintenanceRequests[0].Summary)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaintenanceRequests[0].Priority)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaintenanceRequests[0].Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaintenanceRequests[0].Username)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaintenanceRequests[0].OfficeLocation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.MaintenanceRequests) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Issue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IssueLocation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Summary)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Priority)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OfficeLocation)
            </td>
            <td>
                @*     <a asp-page="./Index" asp-route-id="@{ *@
                @*     Model.SetRejected(item.Id); *@
                @*     }">Reject</a> | *@
                @*     <a asp-page="./Index" asp-route-id="@{ *@
                @*     Model.SetComplete(item.Id); *@
                @*     }">Completed</a> | *@
                @* <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a> *@
                    <form method="post"> 
                        <input type="hidden" name="id" value="@item.Id" />
                        <button type="submit" asp-page-handler="Reject" class="btn btn-primary">
                            Reject
                        </button>
                    </form>
                    <form method="post">
                        <input type="hidden" name="id" value="@item.Id" />
                        <button type="submit" asp-page-handler="Complete" class="btn btn-primary">
                            Completed
                        </button>
                    </form>


            </td>
        </tr>
}
    </tbody>
</table>
